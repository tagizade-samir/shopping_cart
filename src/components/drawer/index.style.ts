import { Utils } from "src/services/utils";

export const styles = {
    header: {
        width: Utils.CONSTANTS.drawerWidth,
        display: 'flex',
        justifyContent: 'flex-end'
    },
    buttonWrapper: {
        display: 'flex',
        flex: 1,
        borderRadius: 0,
        justifyContent: 'flex-end'
    }
}